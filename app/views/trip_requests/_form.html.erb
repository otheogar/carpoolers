<script>
$(function()
{
	$("#trip_request_menu").hide();
	var menu_open = 0;
<!--$("#search_button").button().click(function(event) { 
	if (menu_open == 0)
	{
	 $("#post_menu").fadeOut();
	 $("#search_menu").fadeIn();
	 menu_open = 1;
	}else
	{
     $("#search_menu").fadeOut();
	 menu_open = 0;
	}
	
	});
	
	$("#post_menu").hide(); -->
	$("#trip_request_button").button().click(function(event)
<!--{
	if (menu_open == 0)
	{
	 $("#search_menu").fadeOut();
	 $("#post_menu").fadeIn();
	 menu_open = 1;
	}else
	{
     $("#post_menu").fadeOut();
	 menu_open = 0;
	}
	
	});
	-->
	
	$("#role_div").buttonset();
	$("#trip_request_menu :Accept").button();
	$("#trip_request_menu :Decline").button();
		
})
</script>
<div id="wrapper">
<h1>Your Requests</h1>

<div id = "menu">
<input type="checkbox" id="search_button"/><label for="search_button">Search for Rides</label>
<button id="post_button">Post Ride</button>
<button id="profile_button">Edit Profile</button>
<button id="pending_button">Pending connect Requests</button>
</div>



<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div id="trip_request_menu" class = " ui-corner-all">
<%= form_tag("/trip_request", :method => "get", :remote=>true) do %>  
	<div class="field">
    <%= f.label :from %><br />
    <%= f.label :from_string %>
  </div>
  <div class="field">
    <%= f.label :to %><br />
    <%= f.label :to_string %>
  </div>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.label :date %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.label :time %>
  </div>
  <!-- 
  <div class="field">
    <%= f.label :user_name %><br />
    <%= f.label :user_name %>
  </div>
  -->
  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %> 
</div>
 <% end %> 
  <%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

